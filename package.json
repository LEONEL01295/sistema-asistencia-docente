{
  "name": "sistema-asistencia-docente-biometrica",
  "version": "6.0.0",
  "description": "Sistema web de asistencia docente con portales separados para administración y docentes, preparado para despliegue en Render.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/routes/auth.js && node --check src/routes/teachers.js && node --check public/app.js && node --check public/docente.js",
    "test": "node tests/attendance.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.8.1"
  }
}
